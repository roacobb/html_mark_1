CYC.ui.Listing = (function($){
	var thisObj = this;

    function getListingDetails(listingId) {        
        return true;
    }
    
    function updateListingDetails(listingId, listingInfo) {
    	
    }
    
    function voteup(listingId) {
    	
    }
    
    function votedown(listingId) {
    	
    }
    
    function addComments(listingId, comments) {
    	
    }
    
    function updateComments(listingId, commentId, comments) {
    	
    }
    
    return {
        getListing: getListingDetails,
        updateListing: updateListingDetails,
        voteup: voteup,
        votedown: votedown,
        addComments: addComment
    };
})(jQuery);